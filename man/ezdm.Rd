% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_ezdm.R
\name{ezdm}
\alias{ezdm}
\title{EZ-Diffusion Model}
\usage{
ezdm(mean_rt, var_rt, n_upper, n_trials, links = NULL, version = "3par", ...)
}
\arguments{
\item{mean_rt}{The names of the variable or variables (for 4par version) coding the mean reaction time in seconds in the data.}

\item{var_rt}{The names of the variable or variables (for 4par version) coding the variance of the reaction time in seconds in the data}

\item{n_upper}{The name of the variable coding the number of responses that hit the upper response threshold (typically the number of correct responses) in the data.}

\item{n_trials}{The name of the variable coding the number of trials that was used to calculated the aggregated statistics.}

\item{links}{A list of links for the parameters. For positive parameters
(e.g. \code{bound}, \code{ndt}), "softplus" is available as an alternative to the
default "log" link that grows linearly for large values and avoids the
numerical blow-up of \code{exp()}.}

\item{version}{A character label for the version of the model. There is a three-parameter version
(version = "3par") of the \code{ezdm} that fixes the relative starting point \code{zr} to 0.5, and a
four parameter version (version = "4par"), that allows to freely estimate the starting point.}

\item{...}{used internally for testing, ignore it}
}
\value{
An object of class \code{bmmodel}
}
\description{
EZ-Diffusion Model
}
\details{
\itemize{
\item \strong{Domain:} Decision Making / Response times
\item \strong{Task:} Choice Reaction Time tasks
\item \strong{Name:} EZ-Diffusion Model
\item \strong{Citation:}
\itemize{
\item Wagenmakers, E.-J., Van Der Maas, H. L. J., & Grasman, R. P. P. P. (2007). An EZ-diffusion model for response time and accuracy. Psychonomic Bulletin & Review, 14(1), 3-22. https://doi.org/10/fk447c
}
}
\itemize{
\item Chávez De la Peña, A. F., & Vandekerckhove, J. (2025). An EZ Bayesian hierarchical drift diffusion model for response time and accuracy. Psychonomic Bulletin & Review. https://doi.org/10.3758/s13423-025-02729-y
}
\itemize{
\item \strong{Version:} 4par
\item \strong{Requirements:}

Provide aggregated statistics for each subject and condition that model parameters should vary over:
}
\itemize{
\item Mean reaction times (mean_rt) in seconds
\item Variance of reaction times (var_rt) in seconds
\item Number of responses to the upper decision threshold (n_upper)
\item Total number of trials used to calculate aggregated statistics (n_trials)
}
\itemize{
\item \strong{Parameters:}
\itemize{
\item \code{drift}: Drift rate = Average rate of evidence accumulation of the decision processes
\item \code{bound}: Boundary separation = Distance between the decision boundaries that need to be reached
\item \code{ndt}: Non-decision time = Additional time required beyond the evidence accumulation process
\item \code{zr}: Relative starting point = Starting point between the decision thresholds relative to the upper bound.
\item \code{s}: The diffusion constant, that is the standard deviation of the Gaussian noise during sampling
}
\item \strong{Fixed parameters:}
\itemize{
\item \code{s} = 0
\item \code{mu} = 0
}
\item \strong{Default parameter links:}
\itemize{
\item drift = identity; bound = log; ndt = log; zr = logit; s = log
}
\item \strong{Default priors:}
\itemize{
\item \code{drift}:
\itemize{
\item \code{main}: cauchy(0,1)
\item \code{effects}: normal(0,0.5)
}
\item \code{bound}:
\itemize{
\item \code{main}: normal(0,0.5)
\item \code{effects}: normal(0,0.5)
}
\item \code{ndt}:
\itemize{
\item \code{main}: normal(-1.5,0.5)
\item \code{effects}: normal(0,0.3)
}
\item \code{zr}:
\itemize{
\item \code{main}: normal(0,0.5)
\item \code{effects}: normal(0,0.3)
}
\item \code{s}:
\itemize{
\item \code{main}: normal(0,1)
\item \code{effects}: normal(0,0.3)
}
}
}
}
\examples{
\dontrun{
# Minimal parameter recovery example with 3-parameter EZDM

# Simulate data from known parameters
set.seed(123)
sim_data <- rezdm(
  n = 10,
  n_trials = 100,
  drift = 2,
  bound = 1.5,
  ndt = 0.3,
  version = "3par"
)

# Add subject ID
sim_data$id <- 1:10

# Specify model
model <- ezdm(
  mean_rt = "mean_rt",
  var_rt = "var_rt",
  n_upper = "n_upper",
  n_trials = "n_trials",
  version = "3par"
)

# Specify formula with random effects
formula <- bmf(
  drift ~ 1 + (1 | id),
  bound ~ 1 + (1 | id),
  ndt ~ 1
)

# Fit model (using cmdstanr backend)
fit <- bmm(
  formula = formula,
  data = sim_data,
  model = model,
  backend = "cmdstanr",
  cores = 4,
  chains = 4,
  iter = 2000,
  warmup = 1000
)

# Check parameter recovery
summary(fit)

# Extract population-level effects
# True values: drift = 2, bound = 1.5, ndt = 0.3 (on log scale for drift/bound)
exp(brms::fixef(fit))
}
}
\keyword{bmmodel}
