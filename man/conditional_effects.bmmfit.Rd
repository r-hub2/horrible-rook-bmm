% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_effects.R
\name{conditional_effects.bmmfit}
\alias{conditional_effects.bmmfit}
\alias{conditional_effects}
\title{Conditional Effects for BMM Models}
\usage{
\method{conditional_effects}{bmmfit}(x, par = NULL, scale = c("native", "sampling"), ...)
}
\arguments{
\item{x}{A bmmfit object (created by \code{\link[=bmm]{bmm()}})}

\item{par}{Character string. Name of the model parameter to compute effects for.
This should be one of the parameter names from the original model specification
(see \code{names(x$bmm$model$parameters)}). If \code{NULL} (the default), conditional
effects are computed for all estimated (non-fixed) parameters.}

\item{scale}{Character. Scale on which to show the parameter:
\describe{
\item{\code{"native"} (default)}{Show on natural scale using inverse link transformation.
For example, \code{kappa} with log link shown on exp scale, \code{thetat} with
logit (mixture2p) or softmax (mixture3p) link shown on the probability scale.}
\item{\code{"sampling"}}{Show on the sampling scale (as used during MCMC).
For example, \code{kappa} with log link shown on log scale.}
}}

\item{...}{Additional arguments passed to \code{\link[brms:conditional_effects.brmsfit]{brms::conditional_effects()}}.
Common arguments include:
\itemize{
\item \code{effects}: Character vector specifying which predictor effects to plot
\item \code{conditions}: Named list for setting values of covariates
\item \code{int_conditions}: Conditions for interactions
\item \code{prob}: Probability mass to include in credible intervals (default 0.95)
\item \code{spaghetti}: Logical, whether to add spaghetti lines
\item \code{method}: Method for computing effects ("posterior_predict" or "posterior_epred")
}}
}
\value{
A \code{brms_conditional_effects} object (from brms), which can be:
\itemize{
\item Plotted directly using \code{\link[=plot]{plot()}}
\item Converted to a data frame for custom plotting
\item Combined with other conditional effects plots
}
}
\description{
Compute conditional effects for parameters of a bmmfit object.
This method provides a more intuitive interface than directly calling
\code{\link[brms:conditional_effects.brmsfit]{brms::conditional_effects()}} on bmmfit objects, by:
\itemize{
\item Accepting model parameter names directly (e.g., \code{"kappa"}, \code{"thetat"})
\item Automatically determining whether parameters are distributional or non-linear
\item Optionally applying inverse link transformations to show parameters on their natural scale
}
}
\details{
\subsection{Parameter Types}{

bmm models use two types of parameters internally:
\itemize{
\item \strong{Non-linear parameters (\code{nlpar})}: Core model parameters like \code{kappa}, \code{c}, \code{a}, \code{thetat}
\item \strong{Distributional parameters (\code{dpar})}: Derived parameters used in brms mixture distributions
}

Users should not need to know this distinction - \code{conditional_effects.bmmfit()}
automatically routes to the correct parameter type.
}

\subsection{Scale Transformations}{

By default (\code{scale = "native"}), parameters are shown on their natural scale by
applying inverse link transformations:
\itemize{
\item \code{log} link → exp transformation
\item \code{logit} link → inverse logit (probability scale)
\item \code{tan_half} link → 2*atan transformation (radians)
\item \code{identity} link → no transformation
}

Use \code{scale = "sampling"} to see parameters on the scale used during MCMC sampling.
}
}
\examples{
\dontrun{
# Fit a mixture model with set size effect on kappa
fit <- bmm(
  formula = bmf(kappa ~ 0 + setsize, thetat ~ 1),
  data = zhang_luck_2008,
  model = mixture3p(
    resp_error = "response_error",
    nt_features = paste0("col_lure", 1:5),
    set_size = "setsize"
  )
)

# Get conditional effects for kappa on natural scale (exp of log)
ce_kappa <- conditional_effects(fit, par = "kappa")
plot(ce_kappa)

# Get conditional effects for kappa on log scale (sampling scale)
ce_kappa_log <- conditional_effects(fit, par = "kappa", scale = "sampling")
plot(ce_kappa_log)

# Get effects for thetat (memory probability)
ce_thetat <- conditional_effects(fit, par = "thetat")
plot(ce_thetat)

# Specify which effects to plot
ce_specific <- conditional_effects(fit, par = "kappa", effects = "setsize")

# Combine with other brms options
ce_detailed <- conditional_effects(
  fit,
  par = "kappa",
  effects = "setsize",
  spaghetti = TRUE,
  ndraws = 100
)
}
}
\seealso{
\code{\link[brms:conditional_effects.brmsfit]{brms::conditional_effects()}} for the underlying brms function
}
