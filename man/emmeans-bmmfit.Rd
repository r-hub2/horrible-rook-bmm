% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emmeans.R
\name{emmeans-bmmfit}
\alias{emmeans-bmmfit}
\alias{recover_data.bmmfit}
\alias{emm_basis.bmmfit}
\title{emmeans support for bmmfit objects}
\usage{
\method{recover_data}{bmmfit}(object, ..., dpar = NULL, nlpar = NULL)

\method{emm_basis}{bmmfit}(object, trms, xlev, grid, ..., dpar = NULL, nlpar = NULL)
}
\arguments{
\item{object}{A bmmfit object (created by \code{\link[=bmm]{bmm()}})}

\item{...}{Additional arguments passed to the brmsfit methods.}

\item{dpar}{Character string. Name of the model parameter (e.g., \code{"kappa"},
\code{"c"}, \code{"thetat"}). Automatically resolved to the correct brms parameter
type.}

\item{nlpar}{Character string. Explicit non-linear parameter name. If
provided, takes precedence over \code{dpar}.}

\item{trms, xlev, grid}{Arguments passed by emmeans internally.}
}
\value{
See \code{\link[emmeans:extending-emmeans]{emmeans::recover_data()}} and \code{\link[emmeans:extending-emmeans]{emmeans::emm_basis()}} for return
values.
}
\description{
These S3 methods allow \code{\link[emmeans:emmeans]{emmeans::emmeans()}} to work seamlessly with bmmfit
objects by automatically resolving whether a parameter is a distributional
(\code{dpar}) or non-linear (\code{nlpar}) parameter in brms. Users can always use
\code{dpar = "parameter_name"} regardless of internal classification.
}
\details{
bmm models use two types of parameters internally in brms:
\itemize{
\item \strong{Distributional parameters (\code{dpar})}: Used in models with
\code{custom_family(dpars = ...)} (e.g., SDM, EZDM)
\item \strong{Non-linear parameters (\code{nlpar})}: Used in models with \code{bmf2bf()}
+ \code{nlf()} (e.g., mixture2p, mixture3p, IMM, M3)
}

Users should not need to know this distinction. These methods intercept the
\code{dpar} argument and, if the parameter is actually an nlpar, silently
re-route it to \code{nlpar}.
}
\examples{
\dontrun{
# Works for any bmm model — no need to know dpar vs nlpar
em <- emmeans(fit, ~ condition, dpar = "kappa")
pairs(em)
confint(em)
}

}
\seealso{
\code{\link[emmeans:emmeans]{emmeans::emmeans()}}
}
